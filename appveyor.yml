version: 1.0.{build}

# branches to build
branches:
  # whitelist
  only:
    - master

build:
  project: ./Sources/AutoMonkey/AutoMonkey.sln
  verbosity: minimal
test:
  assemblies: '**\*Test.dll'


